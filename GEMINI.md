# luthas-org-app — Stack: Next.js 15 + App Router + Supabase + Tailwind + shadcn/ui
# Alias: luthas-org

Read `AGENTS.md` for full context. Load `.github/instructions/` by task scope.

## CORTEX Auto-Boot (MANDATORY)
Before any task, run: `npx tsx ../.agent-kb/anvil/cortex-boot.mts --repo=luthas-org-app --agent=181`
Then read `.cortex-boot.json` for session state, pending tasks, model assignments, env locations, and instruction files.
If `.cortex-boot.json` exists and is <1 hour old, reading it is sufficient (skip re-running boot).

## Pre-Commit (see global GEMINI.md for universal gates)
